<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Automatización de Procesos con IA | QivoDigital</title>
  <meta name="description" content="Automatizamos procesos, integraciones y flujos de negocio con IA. Menos tareas manuales, más resultados medibles. Implementación ágil y segura según el alcance." />
  <link rel="canonical" href="https://qivodigital.com/servicios/automatizacion/" />
  <link rel="stylesheet" href="/assets/css/style.css" />
  <style>
    .hero .actions{display:flex;gap:12px;flex-wrap:wrap}
    .hero .actions a{justify-content:center}
    .badges{display:flex;gap:10px;flex-wrap:wrap}
    .qv-chipbar{display:grid;gap:10px;grid-template-columns:repeat(auto-fit,minmax(220px,1fr))}
    .qv-card .cta{display:inline-block;margin-top:6px;font-weight:600}
    .qv-card .cta i{font-style:normal}
  </style>
</head>
<body>

  <!-- HEADER -->
  <div id="header-placeholder"></div>
  <script>
    fetch("/components/header.html")
      .then(r=>r.text())
      .then(h=>document.getElementById("header-placeholder").innerHTML=h);
  </script>

  <!-- HERO -->
  <section class="hero container" id="hero" style="padding-top:92px">
    <div class="grid">
      <div>
        <p class="kicker"><span class="dot"></span>Automatización Empresarial</p>
        <h1>Procesos que corren solos. Resultados que se ven.</h1>
        <p class="lead">Orquestamos bots, integraciones y reglas con <b>IA</b> para que tu operación sea <b>precisa</b>, <b>rápida</b> y <b>medible</b>. 🚀🧠</p>
        <div class="actions">
          <a href="#contacto" class="btn btn-primary">Agenda diagnóstico</a>
          <a href="#beneficios" class="btn btn-ghost">Ver beneficios</a>
        </div>
        <div class="badges">
          <span class="badge">Bots & RPA</span>
          <span class="badge">Workflows con IA</span>
          <span class="badge">APIs & Webhooks</span>
          <span class="badge">Dashboards</span>
        </div>
      </div>
      <div class="hero-card"><p>Automatizaciones QivoDigital — vista previa</p></div>
    </div>
  </section>

  <section class="container" id="beneficios" style="margin-top:32px">
    <h2>Beneficios inmediatos</h2>
    <ul class="qv-list">
      <li>⏱️ Menos tareas repetitivas, más foco estratégico.</li>
      <li>📡 Visibilidad en tiempo real (KPIs y alertas).</li>
      <li>✅ Menos errores humanos y mayor cumplimiento.</li>
      <li>⚡ Despliegue ágil según alcance y dependencias.</li>
    </ul>
  </section>

  <section class="container" style="margin-top:24px">
    <h2>Qué implementamos</h2>
    <div class="qv-stack">
      <article class="qv-card">
        <h3>🤖 Bots operativos</h3>
        <p>Lectura de correos, órdenes, inventarios, exportaciones y conciliaciones con reglas y excepciones controladas.</p>
        <span class="cta">Casos típicos <i>→</i></span>
      </article>
      <article class="qv-card">
        <h3>🔌 Integraciones</h3>
        <p>APIs y Webhooks con ERP/CRM, pagos, mensajería, logística y BI. Retries, colas y trazabilidad end-to-end.</p>
        <span class="cta">Arquitectura de integraciones <i>→</i></span>
      </article>
      <article class="qv-card">
        <h3>📊 Paneles y métricas</h3>
        <p>Datos accionables: ventas, tiempos, SLA, costos y cuellos de botella. Dashboards y alertas para decisiones.</p>
        <span class="cta">Ver métricas clave <i>→</i></span>
      </article>
      <article class="qv-card">
        <h3>🧠 IA en el flujo</h3>
        <p>Clasificar, resumir, decidir y enrutar tareas con modelos inteligentes y políticas de seguridad aplicadas.</p>
        <span class="cta">IA aplicada al proceso <i>→</i></span>
      </article>
    </div>
  </section>

  <section class="container" id="cta" style="margin:40px auto">
    <h2>¿Listo para operar en automático?</h2>
    <p>Diseñamos y desplegamos tu flujo completo con <b>garantías y métricas</b>. El tiempo exacto depende del alcance; trabajamos con herramientas líderes para procesos <b>rápidos, seguros y medibles</b>.</p>
    <div class="actions">
      <a href="#contacto" class="btn btn-primary">Iniciar diagnóstico</a>
      <a href="/servicios/whatsapp/" class="btn btn-ghost">WhatsApp + IA</a>
    </div>
  </section>

  <!-- FORM conectado a /leads.php (public_html) -->
  <section class="container" id="contacto" style="margin-top:24px">
    <form class="qv-form" data-form="automatizacion">
      <h3>Cuéntanos tu proceso a automatizar</h3>
      <div class="qv-grid">
        <label>Nombre*<br><input required name="nombre" placeholder="Tu nombre" /></label>
        <label>Empresa*<br><input required name="empresa" placeholder="Nombre de tu empresa" /></label>
        <label>Email*<br><input required type="email" name="email" placeholder="tucorreo@empresa.com" /></label>
        <label>Área / Interés*<br>
          <!-- Valores PERMITIDOS por leads.php -->
          <select name="interes" required>
            <option value="">Selecciona…</option>
            <option value="automatizacion" selected>Automatización</option>
            <option value="whatsapp">WhatsApp & Chatbots</option>
            <option value="ecommerce">E-commerce</option>
            <option value="software-medida">Software a medida</option>
            <option value="integraciones">Integraciones</option>
            <option value="web-apps">Web & Web-Apps</option>
            <option value="apps">Apps móviles</option>
            <option value="chatbots">Chatbots e IA</option>
            <option value="crm">CRM</option>
            <option value="otros">Otros</option>
          </select>
        </label>
      </div>
      <label>Describe el flujo actual (2–4 líneas)*<br>
        <textarea required name="mensaje" rows="4" placeholder="¿Qué hace hoy tu equipo? ¿Qué debería hacer solo?"></textarea>
      </label>
      <!-- Campos que el recolector espera -->
      <input type="hidden" name="brand" value="qivodigital"/>
      <input type="hidden" name="source_page" value="/servicios/automatizacion/"/>
      <div class="actions" style="margin-top:8px">
        <button class="btn btn-primary" type="submit">Enviar</button>
      </div>
      <small>Tiempo de respuesta &lt; 24 h.</small>
    </form>
  </section>

  <!-- FOOTER -->
  <div id="footer-placeholder"></div>
  <script>
    fetch("/components/footer.html")
      .then(r=>r.text())
      .then(h=>document.getElementById("footer-placeholder").innerHTML=h);
  </script>

  <!-- JS: envío a /leads.php con UTM -->
  <script>
    document.addEventListener('submit', async (e) => {
      const form = e.target.closest('form.qv-form');
      if (!form) return;
      e.preventDefault();

      const fd = new FormData(form);
      // Reforzamos brand y source_page (por si cambian la ruta)
      if (!fd.get('brand')) fd.set('brand','qivodigital');
      if (!fd.get('source_page')) fd.set('source_page', location.pathname);

      // Copia UTM desde querystring si no vienen en el form
      const params = new URLSearchParams(location.search);
      ['utm_source','utm_medium','utm_campaign'].forEach(k=>{
        if (params.get(k) && !fd.get(k)) fd.set(k, params.get(k));
      });

      const endpoint = '/leads.php'; // <-- public_html/leads.php
      try {
        const res = await fetch(endpoint, { method:'POST', body: fd });
        const json = await res.json();
        if (json.ok) {
          form.reset();
          alert('✅ Recibimos tu solicitud. Te contactaremos en menos de 24 h.');
        } else {
          alert('❌ No pudimos registrar tu solicitud. Intenta nuevamente.');
        }
      } catch (err) {
        alert('⚠️ Error de red. Verifica tu conexión e intenta otra vez.');
      }
    });
  </script>

  <script src="/assets/js/main.js"></script>
</body>
</html>
